/*function XO(str) {
  var xovi = 0;
  var oovi = 0;
  str = str.toLowerCase();
  for (i = 0; i < str.length; i++) {
    if (str[i] === "x") {
      xovi++;
    } else if (str[i] === "o") {
      oovi++;
    }
  }
  if (xovi === oovi) {
    return true;
  } else return false;
}*/
